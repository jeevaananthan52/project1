#include<bits/stdc++.h>
using namespace std;
int main()
{
    int x;
    cin>>x;
    while(x--)
    {
        int n;
        cin>>n;
        int e=0;
        int o=0;
        int arr[n];
        for(int i=0;i<n;i++)
        {
            cin>>arr[i];
            if(arr[i]%2==0)
            {
            e+=arr[i];
            }
            else
            {
            o+=arr[i];
            }
        }
        
        if(e%2==0 && o%2==0)
        cout<<"Yes"<<endl;
        else 
        cout<<"No"<<endl;
        
    }
}
NOTE:
Sum of parity  means the sum of all even and odd in array  total sum of both must be in even number
